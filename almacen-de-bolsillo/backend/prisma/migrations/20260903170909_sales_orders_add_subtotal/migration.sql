/*
  Warnings:

  - You are about to drop the column `subtotal_so` on the `sales_orders_so` table. All the data in the column will be lost.

*/
-- AlterTable
ALTER TABLE "sales_orders_so" DROP COLUMN "subtotal_so";
